{
  "name": "buy-sell-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-source stock screening with Claude evaluation, portfolio tracking, and email alerts.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter web dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.0",
    "vitest": "^2.1.8"
  }
}
